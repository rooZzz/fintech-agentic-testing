version: "0.1"
id: credit-lock-disable
goal:
  description: "Disable credit lock from dashboard"
  success: "Credit lock is disabled"

context:
  start_url: "http://localhost:5173/dashboard"

preconditions:
  - instruction: "Create a test user with a plus plan"
  - instruction: "Enable credit lock for the user"

constraints:
  max_steps: 15
  max_cost_usd: 0.40

