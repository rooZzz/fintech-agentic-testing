version: "0.1"
id: credit-lock-persistence
goal:
  description: "Verify credit lock persists when navigating away and returning to dashboard"
  success: "Credit lock remains enabled after navigating away and returning"

context:
  start_url: "http://localhost:5173/dashboard"

preconditions:
  - instruction: "Create a test user with a plus plan"
  - instruction: "Seed 7 loan offers"

constraints:
  max_steps: 25
  max_cost_usd: 0.60

