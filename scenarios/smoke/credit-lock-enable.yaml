version: "0.1"
id: credit-lock-enable
goal:
  description: "Enable credit lock from dashboard"
  success: "Credit lock is enabled"

context:
  start_url: "http://localhost:5173/dashboard"

preconditions:
  - instruction: "Create a test user with a plus plan"

constraints:
  max_steps: 15
  max_cost_usd: 0.40

