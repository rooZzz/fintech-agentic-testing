version: "0.1"
id: get-to-dashboard
goal:
  description: "Login and reach dashboard"
  success: "User is logged in and viewing the dashboard"

context:
  start_url: "http://localhost:5173/login"

preconditions:
  - instruction: "Create a test user with a plus plan"

constraints:
  max_steps: 6
  max_cost_usd: 0.50

